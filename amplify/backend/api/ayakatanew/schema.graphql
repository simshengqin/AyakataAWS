type Task @model {
  id: ID!
  filename: String
  uploadDate: Float
  patientCount: Float
  status2: Int
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}

type Report @model {
  id: ID!
  filename: String
  patientNo: String
  predictedMonths: String
  predictedDate: String
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}
